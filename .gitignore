# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore all environment files (except templates).
# More specific .env ignoring:
.env
.env.*
!.env.example
!.env.sample
# !/.env*.erb # ERBテンプレートのenvファイルは通常使わないため、より明示的な上記で十分

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
# Rails 6以前の古い一時ストレージパス、念のため
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

# Ignore old asset pipeline build artifacts (Propshaft/Importmapでは通常不要だが、念のため)
/public/assets

# Ignore master key for decrypting credentials.
# This file SHOULD NOT be committed to your repository!
/config/master.key

# NOTE: credentials.yml.enc is generally A-OK to commit to Rails Git repositories.
# Rails new default .gitignore usually does not ignore it.
# If you're not comfortable with this, you can uncomment the line below.
# /config/credentials.yml.enc

# Ignore node_modules.
/node_modules/

# Ignore YARN INTEGRITY CHECK files (if using Yarn classic)
.yarn-integrity

# Ignore a local file created by the puma server.
# (もしPumaを直接ローカルで使う場合。Docker内なら不要かも)
puma.sock

# Ignore .byebug_history.
.byebug_history

# Ignore Coverage reports
/coverage/

# Python specific files for the python_api directory
python_api/__pycache__/
# .pyc, .pyo, .pyd
python_api/*.py[cod]
python_api/.pytest_cache/
python_api/.mypy_cache/
python_api/.ruff_cache/
# Common virtual environment directory names
python_api/.venv/
python_api/env/
python_api/venv/
python_api/ENV/
python_api/env.bak/
python_api/venv.bak/
# For Flask instance folders, if used
python_api/instance/

# OS-generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor/IDE specific
.idea/
.vscode/
*.swp
*~
*.sublime-project
*.sublime-workspace

# Docker specific (if any local Docker files should be ignored from git)
# Example: docker-compose.override.yml if it's only for local use
# docker-compose.override.yml

# Temporary or backup files
*.bak
*.tmp
*.temp
/app/assets/builds/*
!/app/assets/builds/.keep
