<%# ヘッダーに表示する動的なタイトルを設定 %>
<% content_for :header_title, "練習完了！" %>

<%# --- メインコンテンツ --- %>
<div class="container mx-auto px-4 py-4 max-w-sm">
  <p class="text-center text-gray-600 mb-6">おつかれさまでした！今回の練習結果です</p>

  <%# --- 総合評価カード --- %>
  <div class="bg-white rounded-2xl shadow-lg p-6 mb-6 text-center">
    <h3 class="text-sm text-gray-500 mb-2">総合評価</h3>
    
    <%# --- 総合スコア表示 --- %>
    <div class="relative w-32 h-32 mx-auto flex items-center justify-center">
      <div class="absolute inset-0 border-8 border-purple-100 rounded-full"></div>
      <div class="absolute inset-0 border-8 border-transparent border-t-purple-600 rounded-full animate-spin" style="animation-duration: 2s; transform: rotate(45deg);"></div>
      <span class="text-4xl font-bold text-purple-700"><%= @practice_session_log.total_score || 'N/A' %></span>
    </div>
    
    <%# --- レーダーチャート表示エリア --- %>
    <div class="mt-4">
      <%# ★★★ 注意 ★★★ %>
      <%# レーダーチャートの描画には、Chart.jsなどのJavaScriptライブラリの導入と、
          それ用のStimulusコントローラー作成が必要です。
          これは今後のタスクとし、ここではプレースホルダー（ダミー画像など）を表示します。 %>
      <div class="w-full h-48 bg-gray-100 flex items-center justify-center text-gray-400 rounded-lg">
        <p>（レーダーチャート表示エリア）</p>
      </div>
      <div class="flex justify-center gap-x-6 text-sm mt-2">
        <span class="inline-flex items-center"><span class="w-3 h-3 rounded-full bg-purple-400 mr-2"></span>今回のスコア</span>
        <span class="inline-flex items-center"><span class="w-3 h-3 rounded-full bg-red-300 mr-2"></span>前回のスコア</span>
      </div>
    </div>
  </div>

  <%# --- ランキングカード --- %>
  <div class="bg-white rounded-2xl shadow-lg p-6 mb-6">
    <h3 class="text-lg font-semibold text-gray-700 mb-4 text-center">ランキング</h3>
    <div class="space-y-3">
      <%# ★★★ 注意 ★★★ %>
      <%# 全ユーザーを含めたランキング機能は、別途バックエンドでの集計ロジックが必要です。
          ここではデザイン見本に合わせて、現在のユーザーのスコアのみをランキング風に表示します。 %>
      <div class="flex items-center justify-between p-3 rounded-lg bg-purple-50">
        <div class="flex items-center">
          <span class="text-lg font-bold text-gray-400 w-8 text-center">?</span>
          <span class="w-8 h-8 rounded-full bg-gray-200 mx-3"></span> <%# ユーザーアイコンのプレースホルダー %>
          <span class="font-medium text-gray-800">あなた</span>
        </div>
        <div class="text-2xl font-bold text-purple-700">
          <%= @practice_session_log.total_score || '--' %>
        </div>
      </div>
    </div>
  </div>

  <%# --- アクションボタン --- %>
  <div class="space-y-3">
    <%= link_to 'ホームに戻る', root_path, class: "block w-full text-center px-6 py-4 text-base font-semibold text-white bg-purple-600 rounded-lg shadow-md hover:bg-purple-700 transition" %>
    <%# 「学習記録を確認」と「結果をシェア」は将来的な機能のため、一旦ダミーのリンクにしておきます %>
    <%= link_to '学習記録を確認', '#', class: "block w-full text-center px-6 py-4 text-base font-semibold text-purple-700 bg-white border border-purple-600 rounded-lg shadow-md hover:bg-purple-50 transition" %>
    <%= link_to '結果をシェア', '#', class: "block w-full text-center px-6 py-3 text-sm text-gray-500 hover:text-gray-700 transition" %>
  </div>
</div>